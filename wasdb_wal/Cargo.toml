[package]
name = "wasdb_wal"
version = "0.1.0"
edition = "2024"

[dependencies]
parking_lot.workspace = true
thiserror.workspace = true
wasdb_storage = { version = "0.1.0", path = "../wasdb_storage" }
wasdb_tx = { version = "0.1.0", path = "../wasdb_tx" }

[dev-dependencies]
tempfile = "3.27.0"
